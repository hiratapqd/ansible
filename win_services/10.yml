---
- hosts: win

  tasks:

  - name: Check Themes
    win_service:
      name: Themes
    register: result
    failed_when: " result.state != 'running'"
#      ignore_errors: yes


  - mail:
      host: smtp.gmail.com
      port: 587
      username: hiratapqd@gmail.com
      password: kozevingqstzgtsb
      to: Sergio Hirata <hiratapqd@yahoo.com.br>
      subject: '[TESTE] Ansible-report'
      body: 'System {{ result.state }} has been successfully provisioned.'
    delegate_to: localhost


  - name:
    debug:
      msg: "{{ result.state }}"

#  - debug: msg="running"
#    when: result.state is not defined or result.name is not defined
